# Split polygon using buffer method (fallback)

Split polygon using buffer method (fallback)

## Usage

``` r
split_by_buffer(polygon, line, crs)
```

## Arguments

- polygon:

  Polygon to split

- line:

  Line to split by

- crs:

  CRS

## Value

List with side1 and side2
